/* Required notice: Copyright (c) 2024, Will Shown <ch-ui@willshown.com> */

@layer tokens, utilities, elements;

@layer tokens {
  @chui app-astro;
}

:root {
  font-size: 16px;
  font-family: var(--ch-font-text);
  background: var(--ch-bg-base);
  color: var(--ch-fg-base);
}
